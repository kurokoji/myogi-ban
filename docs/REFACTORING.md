# Refactoring Backlog

このファイルをリファクタリングの進捗管理に使用する。

- 着手前に未完了の項目 `[ ]` を選ぶ。
- `AGENTS.md` のTDD手順に従い、Red–Green–Refactorで進める。
- 全検証が通ったら項目を `[x]` にし、実装と同じコミットへ含める。
- 必要に応じて項目を分割し、新しいIDを追加する。

## Completed

- [x] R001 縦横比固定の計算を純粋関数へ分離する
- [x] R002 レイアウト名の正規化・重複判定を共通化する
- [x] R003 レイアウト形式の移行処理を補完処理から分離する
- [x] R004 レイアウト保存データの生成を純粋関数へ分離する
- [x] R005 レイアウト削除後の遷移先選択を純粋関数へ分離する

## High Priority

- [x] R006 ボタン一括編集で変更項目だけを反映する規則をテストする
- [x] R007 ボタン一括編集で未変更項目を維持する規則をテストする
- [x] R008 ボタン一括編集で `undefined` の個別設定を削除する規則をテストする
- [x] R009 ボタン一括編集で不足しているボタン設定を補完する規則をテストする
- [x] R010 ボタン一括編集で選択が空なら何もしない規則をテストする
- [x] R011 レイアウトRepositoryの名前検証を共通化し、空名とパストラバーサルを拒否する
- [x] R012 保存エラーを型で表現し、HTTPステータス文字列の解析を廃止する
- [x] R013 レイアウトのdirty判定を純粋関数へ分離する
- [x] R014 画像アップロード対象の更新処理を純粋関数へ分離する
- [x] R015 `GamepadView` の矩形交差判定を純粋関数へ分離する
- [x] R016 `GamepadView` のCtrl/Cmdクリックによる選択追加・解除を純粋関数へ分離する
- [x] R017 `GamepadView` の単体ドラッグ座標計算を純粋関数へ分離する
- [x] R018 `GamepadView` の複数選択ドラッグ座標計算を純粋関数へ分離する
- [x] R019 `GamepadView` を背景・スティック・ボタン・選択表示へ分割する
- [x] R020 `ButtonSettingsPanel` を既定設定・選択設定・画像・外観へ分割する
- [x] R021 `editor.tsx` のボタン追加・削除操作を純粋関数へ分離する
- [x] R022 `editor.tsx` の選択状態補正を純粋関数へ分離する
- [x] R023 インポートしたレイアウトを実行時に検証する
- [x] R024 壊れたレイアウトJSONのエラー処理と復旧規則を定義する
- [x] R025 レイアウト保存を一時ファイルとrenameによる原子的書き込みへ変更する

## Medium Priority

- [x] R026 エディターとViewerのゲームパッド監視ループを共通化する
- [x] R027 `GamepadManager` にイベントリスナーを解除するライフサイクルを追加する
- [x] R028 軸割り当てコードのencode・decodeを純粋関数と専用型へ分離する
- [x] R029 `useLayoutHistory` のスナップショット比較をテスト可能な関数へ分離する
- [x] R030 dirty判定と履歴判定のスナップショット生成を共通化する
- [x] R031 ガイド操作のポインター座標変換を純粋関数へ分離する
- [x] R032 ガイドの移動・範囲外削除規則を純粋関数へ分離する
- [x] R033 プレビュー倍率の上限・下限・増減計算をテストする
- [x] R034 定規の目盛り計算を純粋関数へ分離する
- [x] R035 画像アセットの同名衝突規則を定義する
- [x] R036 画像アップロードのMIME・サイズ・ファイル名を検証する
- [x] R037 `local-server.ts` を状態・レイアウト・画像ルートへ分割する
- [x] R038 APIの成功・失敗レスポンス形式を統一する
- [x] R039 デフォルト指定が存在しない場合のフォールバックを共通化する
- [x] R040 Electronの開発・製品データ保存先決定を共通関数へ分離する
- [x] R041 Electronのサーバー終了とPID削除を単一cleanupへ集約する
- [x] R042 開発監視スクリプトでサーバー変更時の再起動を扱う
- [x] R043 開発監視スクリプトの子プロセス異常終了とシグナル処理をテストする
- [x] R044 ViewerのAPI取得失敗・WebSocket切断・再接続状態を整理する
- [x] R045 `document.getElementById` によるプレビュー参照をReact refへ統一する
- [x] R046 `selectedButtonIndex` と `selectedButtonIndexes` を単一の選択モデルへ統合する
- [x] R047 最大ボタン数・既定寸法・ポーリング間隔などのマジックナンバーを定数化する

## Lower Priority

- [x] R048 日本語・英語翻訳を別ファイルへ分割する
- [x] R049 翻訳キーを型付けし、言語間の不足を検出する
- [x] R050 CSSカスタムプロパティ用の型を定義し、型キャストを集約する
- [x] R051 未使用の画像スティック用CSSを削除する
- [x] R052 テストランナーで個別テスト名とassert差分を見やすくする
- [x] R053 保存名重複・チェーンボタン・複数選択のコンポーネントテスト基盤を追加する
- [x] R054 Web版で読込・編集・保存・Viewer反映の最小E2Eテストを追加する
- [x] R055 複数ボタンのドラッグ位置更新を一括化し、O(k×n)からO(n+k)へ改善する
